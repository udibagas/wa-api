export type AppType = {
  id: number;
  name: string;
  description: string;
};

export type GroupType = {
  id: number;
  name: string;
  description: string;
};

export type RecipientType = {
  id: number;
  name: string;
  phoneNumber: string;
};

export type UserType = {
  id: number;
  name: string;
  email: string;
};
